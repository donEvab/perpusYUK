# Local MySQL fallback
DB_HOST=localhost
DB_PORT=3306
DB_NAME=perpustakaan_db
DB_USER=root
DB_PASSWORD=

# Optional direct JDBC URL, useful on custom hosting:
# DB_URL=jdbc:mysql://host:3306/perpustakaan_db

# Optional platform URL, useful if the provider exposes a mysql:// URL:
# DATABASE_URL=mysql://user:password@host:3306/perpustakaan_db
